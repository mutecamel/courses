[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mypkg"
version = "0.1.0"
authors = [
    {name = "Your Name", email = "your_email@example.com"}
]
description = "A sample package for demonstrating package creation"
requires-python = ">=3.12"
